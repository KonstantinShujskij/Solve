const CREATE = 'CREATE'
const CHECK = 'CHECK'
const CANCLE = 'CANCEL'
const SEARCH = 'SEARCH'
const RESERVE = 'RESERVE'
const PACT = 'PACT'
const CONFIRM = 'CONFIRM'


module.exports = {
    status: { CREATE, CHECK, CANCLE, SEARCH, RESERVE, PACT, CONFIRM }
}
