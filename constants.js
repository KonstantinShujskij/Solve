const SEARCH = 'SEARCH'
const RESERVE = 'RESERVE'
const CHECK = 'CHECK'
const CANCLE = 'CANCEL'
const PACT = 'PACT'
const CONFIRM = 'CONFIRM'

module.exports = {
    status: { SEARCH, RESERVE, CHECK, CANCLE, PACT, CONFIRM }
}
